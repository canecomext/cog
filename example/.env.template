# Database Configuration
# =====================

# Option 1: Database connection URL (most common)
DATABASE_URL=postgresql://username:password@localhost:5432/example_db

# Option 2: Individual connection parameters
# Uncomment these if you prefer to specify connection details separately
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=example_db
# DB_USER=username
# DB_PASSWORD=password

# SSL/TLS Configuration (optional)
# ================================

# Path to SSL certificate file (if required by your database)
# DB_SSL_CERT=/path/to/client-cert.pem

# Path to SSL key file (if required)
# DB_SSL_KEY=/path/to/client-key.pem

# Path to CA certificate (if required)
# DB_SSL_CA=/path/to/ca-cert.pem

# SSL mode: disable, require, verify-ca, verify-full
# DB_SSL_MODE=require

# Application Configuration
# ========================

# Server port
PORT=8000

# Environment: development, staging, production
NODE_ENV=development

# Enable debug logging
DEBUG=false

# API Configuration
# ================

# API base path
API_BASE_PATH=/api

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# PostGIS Configuration (if using spatial features)
# =================================================

# SRID for spatial operations (default: 4326 for WGS84)
DEFAULT_SRID=4326